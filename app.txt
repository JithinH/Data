import pandas as pd
import streamlit as st

from PIL import Image

st.set_page_config(page_title='survey results')
st.header('survey results 2021')
st.subheader('hello')

excel_file='Test.xlsx'
sheet_name='test'

df=pd.read_excel(excel_file,
                sheet_name=sheet_name,
                usecols='A:D',
                header=0)
                
st.dataframe(df)

mask=(df['City'].where(df['State[c]']== 'Florida'))
makers=df['City'].where(df['State[c]']== 'Florida')
st.dataframe(makers.dropna())
